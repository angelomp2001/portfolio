# Docker Compose configuration for project_2 data analysis application
services:
  analysis:
    build:
      context: .
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./docs:/app/docs
