templates:
readme.md:
# Title


## Business Case
(case)


## Task
(brief description of process involving target, features, model, etc..  eg. Predict `Price` using structured car listing data. List multiple tasks in bullet point format.)


## Project Summary and Findings
(summary, results)


### Models Evaluated
| Base Model            | RMSE    | Train Time (s) | Pred Time (s) |
|-----------------------|---------|----------------|---------------|
| LinearRegression      | 2724.06 | 0.26           | 0.025         |
| LGBMRegressor         | 2063.19 | 2.22           | 0.041         |
| RandomForestRegressor | 2176.82 | 1.59           | 0.040         |
| CatBoostRegressor     | 2026.19 | 1.09           | 0.002         |
| XGBRegressor          | 2236.42 | 14.43          | 0.003         |




## Environment
Dev ✅ # staging, production


## Project folder structure/
├── data/
│   └── data.csv                       # Raw dataset
├── docs/
│   ├── agent_readme.md                # Experiment branching workflow
│   ├── EXPERIMENTS.md                 # Log of all experiments run
│   ├── RESULTS.md                     # Results summary for current branch
│   ├── Branch_Edit_Summary.md         # Detailed code changes for current branch
│   └── experiment_log.md              # Running log of all merged experiments
├── models/
│   ├── best_model.
│   └── best_model_metadata.
├── src/
│   ├── data_preprocessing.py          # EDA, cleaning, encoding, scaling
│   └── model_training.py              # Model training and evaluation
├── catboost_info/                     # Auto-generated by CatBoost (not tracked)
├── main.py                            # Entry point — orchestrates the pipeline
├── app.py                             # API
├── eda.ipynb                          # Jupyter notebook (exploratory work)
├── requirements.txt                   # pip install requirements
├── .gitignore                         # list of things for github to ignore
└── README.md                          # This file


## Project documentation
Readme: the main project document
Agent_readme: git strategy and workflow for the agent
Branch_Edit_Summary: a description of the edits done on this branch.
Checklist: a generic template checklist of what features should be in this project
Experiment_log: a log of the results from the branch.
Results: the latest results of the branch, just before it merges with main.
EXPERIMENTS: a cumulative log of experiment logs that lives on the main branch only. 


To know what is happening, read the readme, then the results.  If the results are inadequate, read the experiments.md to see how far along we are.  The latest experiment_log and/or branch_edit_summary will tell you where we are right now. From this, you can guess what to do next to complete the branch experiment and complete the project.  


## How to train model
```bash
python main.py
```


## How to run inference via API
(instructions on uvicorn, uvicorn api:app --reload, etc.)




## Data Pipeline Overview
├── Load — Read `data.csv`, sample 10,000 rows (optional)
├── Clean — Review each column, fix/remove bad values, feature engineer
├── Split — Split data via cross-validation, and apply the following:
│   ├── Encode  — convert text values into numerical values
│   ├── Scale — standardize numerical values
│   ├── Train — Fit models
│   ├── Evaluate — Compare metric, train time, prediction time across models
│   └── Tune — Hyperparameter search on best model
├── Test - Test best model
└── Save — Save best model  


## API Overview (if applicable)
├── Load — Load best model
├── Predict — Apply model to input API data
├── Save — Save Results  




## Images
(links to images saved in the project)



Project Checklist template
# global config
[ ] Global random seed # if a random seed is used anywhere, set RANDOM_STATE and apply ubiquitously.


# clean data
[ ] Store data types # num_cols, cat_cols = data_types(): df.select_dtypes(include='number').columns, etc.
[ ] Handle missing values # eg. df[num_cols], df[cat_cols] = df[num_cols].dropna(), df[cat_cols].fillna('missing') 
[ ] Remove duplicates # eg df.drop_duplicates(inplace = True) 


# data artifacts
[ ] Save raw data stats # save_stats(path=docs/data_stats.json). {timestamp, shape, **df.columns.describe(include="all")}.
[ ] Save raw data image: univariate histogram/bar chart # save_charts(path=docs/data_charts.png) based on num/cat dtype.
[ ] Save clean data stats: save_stats(path=docs/data_stats.json). {timestamp, shape, **df.columns.describe(include="all")}.
[ ] Save clean data image: pair-wise and univariates grid #  sns.pairplot(num_cols), for col in cat_cols: sns.countplot(x=df[col])
[ ] Save best model # save_model(weights: models/.keras, pipeline: models/.joblib)


# load data
[ ] Sampling #  load_data(, sample = n), df_raw = df_raw.sample(...=RANDOM_STATE)
[ ] Split data: train/val vs test # train_test_split(...test_size=TEST_RATIO, random_state=RANDOM_STATE)


# column preprocessing
[ ] Label target: if there is a target column, make sure it’s labeled. in config area # target = 
[ ] Time data as datetime: # clean_data(): df[datetime_cols] = pd.to_datetime(df[datetime_cols], format="%Y-%m-%d") 
[ ] Feature engineer datetime: hour/day/month/weekday/lag features # Pipelines(): ….dt.hour/day/month/weekday/shift(n). 
[ ] Apply PolynomialFeatures: where it makes sense. # Pipelines(): PolynomialFeatures(degree=2, include_bias=False)
[ ] Cross validation with holdout: KFold, StratifiedKFold, TimeSeriesSplit # train_model(): apply for all projects that train models
[ ] Pipelines: For all projects that train models. # Pipelines(): Pipeline(steps=[('preprocessor', preprocessor),('model', a_model)])
[ ] Model-based Encoding: # Pipelines(): if is_tree: Col_Trans('cat',OrdinalEncoder(), 'num', 'passthrough'), else: Col_Trans('cat', OneHotEncoder(),'num', Pipeline(StandardScaler(),'poly',PolynomialFeatures(),'scale2', StandardScaler())
[ ] Feature scaling: # Pipelines(): MinMaxScaler for bounded values, StandardScaler/RobustScaler otherwise. 


# models
[ ] Hyperparameter grid: in config area # MODELS = [(name, model_instance, is_tree, hyperparam_grid)]
[ ] Dummy models: naive estimates (0, 1/n_classes, 1, mean, median) # baseline_model_score(y_train, naive_estimate, metric)
[ ] Classifier models: if relevant: Logistic, LGBM, RandomForest, CatBoost, XGB, Keras # in config area
[ ] Linear models: if relevant: Linear, LGBM, RandomForest, CatBoost, XGB, KerasNN # in config area


# model training
[ ] model training: one function for all models, including keras # train_model(): KerasRegressorWrapper(build_fn=build_keras_model())
[ ] Training visualizations: timeseries, print chart of model training iterations # train_model(): training_chart()
[ ] training statistics: duration # save: branch_experiment, readme # train_model(): training_stats()
[ ] Early Stopping parameters: if applicable # KerasRegressorWrapper.fit(): EarlyStopping 
[ ] Dropout parameter: if applicable # KerasRegressorWrapper.fit(): build_fn(...dropout_rate)
[ ] Model checkpoints: if applicable # KerasRegressorWrapper.fit(): ModelCheckpoint
[ ] Learning rate adaptation: if applicable # KerasRegressorWrapper.fit(): ReduceLRonPlateau


# model choice
[ ] Include all evaluation Metrics: MAE, MSE, RMSE, R2, Accuracy, Precision, recall, F1, Confusion matrix, ROC AUC, PR AUC, Log loss
[ ] Random Search: for hyper/parameter grid search # HalvingRandomSearchCV or RandomizedSearchCV
[ ] Random Forest: Illustrate feature importance # if RF used, save_rf_important_features(docs/random_forest_important_features.md)
[ ] Decision Tree: visual of the decision tree model # if applicable, save_decision_tree(docs/decision_tree_model.png)
[ ] Component diagram (DAG) # run DAG.py file. 


# API
[ ] API: load best model, run inference, log and save run and results # if there is a model, apply


# testing.py
[ ] Unit testable # apply if functions or classes are created.


main.py:

def main(
    sample_size: int | None = None,
    k_folds: int = 5,
    random_state: int = 42,
    metric: str = "f1",
):
    # 1. Load and explore data
    df_raw = load_data(sample=sample_size)
    save_stats(df_raw, label="raw")
    save_charts(df_raw, label="raw")

    # 2. Clean data
    df = clean_data(df_raw)
    save_stats(df, label="clean")
    save_charts(df, label="clean")

    # 3. Train / test split
    X = df.drop(TARGET_COL, axis=1)
    y = df[TARGET_COL]

    X_train, X_test, y_train, y_test = train_test_split(
        X,
        y,
        test_size=0.2,
        random_state=random_state,
        stratify=y,
    )

    # 4. Data types for preprocessing
    num_cols, cat_cols = data_types(X)

    # 5. Build pipelines, run CV, collect results
    pipelines: dict[str, Pipeline] = {}
    hyperparam_grids: dict[str, dict] = {}
    cv_results: dict[str, dict] = {}

    for name, model, is_tree, grid in MODELS:
        preprocessor = column_preprocessor(
            num_cols=num_cols,
            cat_cols=cat_cols,
            is_tree=is_tree,
        )

        pipelines[name] = Pipeline(
            steps=[
                ("preprocessor", preprocessor),
                ("model", model),
            ]
        )

        hyperparam_grids[name] = grid

        cv_results[name] = train_model(
            pipeline=pipelines[name],
            X_train=X_train,
            y_train=y_train,
            k_folds=k_folds,
            random_state=random_state,
            metric=metric,
            param_grid=hyperparam_grids[name],
        )

    # 6. Pick best model by chosen metric
    best_model_name = max(
        cv_results,
        key=lambda name: cv_results[name][metric]
    )
    best_pipeline = pipelines[best_model_name]

    # 7. Optionally: hyperparameter tuning for the best model
    if hyperparam_grids[best_model_name]:
        best_pipeline_tuned = tune_model(
            pipeline=best_pipeline,
            X_train=X_train,
            y_train=y_train,
            metric=metric,
            param_grid=hyperparam_grids[best_model_name],
        )
    else:
        best_pipeline_tuned = best_pipeline

    # 8. Fit best pipeline on full training data
    best_pipeline_tuned.fit(X_train, y_train)

    # 9. Save model + CV results
    save_model(
        model=best_pipeline_tuned,
        model_name=best_model_name,
        metadata={
            "metric": metric,
            "cv_results": cv_results[best_model_name],
            "random_state": random_state,
            "k_folds": k_folds,
            "num_cols": num_cols,
            "cat_cols": cat_cols,
        },
    )

    # 10. Final test evaluation
    test_results = test_model(
        model=best_pipeline_tuned,
        X_test=X_test,
        y_test=y_test,
        metric=metric,
    )

    # 11. Save test results (and optionally all CV results)
    save_results(
        results={
            "test": test_results,
            "cv": cv_results,
            "best_model_name": best_model_name,
            "metric": metric,
        },
        model_name=best_model_name,
    )


if __name__ == "__main__":
    main()